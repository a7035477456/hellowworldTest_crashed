# Server (align with HAProxy/PM2: port 40000 on each webserver)
PORT=40000
NODE_ENV=production

# PostgreSQL (Primary/Replica: set DB_HOST per server or use DATABASE_URL)
DB_HOST=localhost
DB_PORT=50010
DB_NAME=vsingles
DB_USER=postgres
DB_PASSWORD=w2332w2332w

# Optional: single connection string (overrides DB_* if present in app code)
# DATABASE_URL=postgres://postgres@localhost:5432/yourdb

# Auth (required in production)
JWT_SECRET=

# Email (nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Twilio (phone verification)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_ServiceSID=
